#if host, call HostSession which will spin up a new GameSession Object
#make sure the chosen port is open for connection
#immediately connect and join on that port so that the host is treated the same as a joined player.
